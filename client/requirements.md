## Packages
date-fns | Formatting dates for order history and displays
lucide-react | Icons for the UI

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["var(--font-sans)"],
  display: ["var(--font-display)"],
}

Mock Auth: Storing a simple "isAuthenticated" flag in localStorage.
Mock Email: Backend handles sending emails; frontend only updates the status.
